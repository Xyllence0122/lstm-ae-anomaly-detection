# V5 最終模型、Locked Holdout與邊緣部署結果

## 1. 結論

V5保留Sliding-Window LSTM Autoencoder主模型，沒有改成Forecaster或
Isolation Forest。V3.2/V4保持不可覆寫，V5另用全新selection、normal-only
calibration及locked holdout seeds完成最後一次受控優化。

V5在一次性paired locked holdout通過所有預先鎖定門檻。相較同一批新
holdout上的凍結V3.2，V5主要改善快速暫態到位異常（Type A），但正常事件
false positives由1增加為4。這是Recall與誤報之間的實際trade-off，不能只
報告Recall而省略FPR。

## 2. 方法變更

V5的模型架構仍為LSTM-AE，輸入仍是因果raw、per-sample difference及
sample progress特徵。方法變更為：

1. 新增4點短視窗，最終profiles為4/16/64點。
2. profiles可各自選擇及校準raw/delta feature group。
3. normal reconstruction loss將delta feature權重設為2.0、elapsed phase
   權重設為0.25、時間差分loss權重設為0.5。
4. 仍採normal-only threshold calibration及嚴格`score > threshold`。
5. 最終三個profiles均由selection程序選為raw group；這是保存的實驗結果，
   不應反向改寫成「V5最終使用delta group」。delta feature仍參與模型輸入
   與加權normal reconstruction訓練。

完整預註冊協定見`V5_實驗計畫.md`。

## 3. Selection與Calibration

Selection資料：

- train normal：1,500，seed `710101`。
- validation normal：3,000，seed `710102`。
- validation anomaly：A/B/C各400，seed `710103`。
- training seeds：`710201/710202/710203`，每個30 epochs。
- release：seed `710201`、epoch 10。

Selection release結果：

- Precision：99.71%。
- Recall：86.75%。
- F1：92.78%。
- FPR：0.10%。
- A/B/C Recall：79.25% / 93.0% / 88.0%。

三個seeds的10/20/30 epoch共九個checkpoints均通過預註冊gate。Selection
結果只用於模型選擇，不是最終泛化成績。

Normal-only calibration使用10,000 normal、seed `720101`，沒有存取異常
標籤。Threshold由`1.0230547836`校準為`1.1915203284`，觀察到10/10,000
event scores嚴格高於threshold，符合0.1%目標。

## 4. 一次性Paired Locked Holdout

Locked cohort在模型、profiles與threshold封存後才由seed `730101/730102`
產生：10,000 normal及A/B/C各1,000 anomaly。V5與凍結V3.2在完全相同
cohort配對比較。

| 指標 | V5 | 凍結V3.2 | V5變化 |
|---|---:|---:|---:|
| Accuracy | 95.07% | 92.11% | +2.96 pp |
| Precision | 99.83% | 99.95% | -0.12 pp |
| Recall | 78.77% | 65.83% | +12.93 pp |
| F1 | 88.06% | 79.38% | +8.68 pp |
| FPR | 0.04% | 0.01% | +0.03 pp |
| AUC | 97.57% | 96.50% | +1.07 pp |
| Type A Recall | 69.0% | 44.3% | +24.7 pp |
| Type B Recall | 82.9% | 72.1% | +10.8 pp |
| Type C Recall | 84.4% | 81.1% | +3.3 pp |

V5 confusion counts：TP 2,363、FN 637、FP 4、TN 9,996。Recall 95%
Wilson interval為77.27%至80.19%；FPR 95% Wilson interval為0.0156%至
0.1028%。Type A Recall 95% Wilson interval為66.07%至71.79%。

配對事件中，V5獨自抓到444件異常，V3.2獨自抓到56件，兩者都抓到1,919
件，兩者都漏掉581件。這支持V5改善，但637/3,000異常仍被漏掉，因此不得
宣稱量產可用或接近100%偵測。

## 5. 低異常盛行率解讀

依locked Recall/FPR推估：

- 異常盛行率1%時，V5 projected precision為95.21%。
- 異常盛行率0.1%時，V5 projected precision為66.34%。

在0.1%盛行率下，凍結V3.2因FPR更低而有86.82% projected precision，
但其Recall只有65.83%。因此V5並非所有指標全面優於V3.2；V5選擇較高
Recall並接受仍低於0.1%預算的FPR增加。實際工廠操作點必須依異常成本、
人工覆核能力及recipe分布重新評估。

## 6. 偵測時間

V5已偵測事件的latency中位數為20 samples，p95為68 samples。來源cadence
約1 Hz，因此可近似解讀為20秒及68秒，但這不是相對晶圓損傷或事故的lead
time；資料沒有yield loss、damage或事故發生時間標籤。

`detected_before_injection_end_rate`為99.96%，只代表在合成intervention
support結束前越過門檻，不代表在真實損害發生前預警，不應作為主要成效。

## 7. Edge Runtime

V5輸出獨立TorchScript及manifest，不修改或破壞V4 package：

- 40條selection parity序列。
- 8,944個profile score比較。
- eager與TorchScript輸出最大差：0。
- 離線與逐筆runtime profile score最大差：`9.03e-7`。
- alarm decision mismatch：0。
- 所有首次告警sample一致。

本機100條合成normal benchmark處理10,292筆sample，推論p99約2.65 ms，
但狀態為`non_pi_host_development_measurement`，不能稱為Pi 5結果。V5尚未
在實體Pi 5量測；V4的Pi 5正式數字不能移植到V5。

Pi 5更新分支後執行：

```bash
git pull origin edge-early-warning_v5
python 32_build_v5_deployment.py
python 33_benchmark_v5_runtime.py \
  --output outputs/v5/pi5_benchmark_v5.json \
  --sequences 1000 \
  --torch-threads 1
vcgencmd get_throttled
```

## 8. 論文可用主張與限制

可以主張：

- 在相同新合成holdout與normal-only calibration下，V5相對凍結V3.2改善
  整體及Type A Recall，且FPR保持在預註冊0.1%上限內。
- V5 TorchScript逐筆runtime與離線計算具有數值及告警parity。
- 架構在約1 Hz資料下具備即時推論可行性；V5實體Pi數字需另行量測。

不得主張：

- 95.07% Accuracy是實際半導體工廠準確率。
- 已證明提高良率、避免事故或達到量產要求。
- synthetic locked holdout是獨立真實外部驗證。
- 目前約1 Hz資料能驗證0.1秒或0.001秒異常。
- 既有43 normal加20 faulty歷史重用資料是unseen external data。

正式報告為`outputs/v5/locked_holdout_v5.json`，SHA-256為
`7c323d2213b4a486eb0c743d0c0a643a5226d5795e422fef0eae5a8b7bb6d0e8`。

# V5 最終模型優化實驗計畫

## 1. 目的與邊界

V5保留Sliding-Window LSTM Autoencoder主模型，目標是在不提高正常事件
誤報預算的前提下，改善V3.2對快速暫態到位異常（Type A）的Recall。
V3.2/V4權重、threshold、locked holdout及Pi 5正式量測均保持不可覆寫。

V5仍是論文用原型，不是量產安全連鎖系統。合成資料由真實normal資料的
statistics/profile產生，因此不能稱為獨立真實外部驗證。

## 2. 預先鎖定的資料協定

- 訓練normal：1,500條，seed `710101`。
- selection normal：3,000條，seed `710102`。
- selection anomaly：A/B/C各400條，共1,200條，seed `710103`。
- normal-only calibration：10,000條，預留seed `720101`。
- locked holdout normal：10,000條，預留seed `730101`。
- locked holdout anomaly：A/B/C各1,000條，預留seed `730102`。

只有selection cohort可用於權重、epoch、視窗、score mode、feature group及
persistence選擇。只有selection gate通過後才可使用normal-only calibration
seed。calibration完成後才允許執行一次locked holdout。

## 3. 預先鎖定的方法變更

1. 視窗由V3.2的`8/16/32/64`擴充為`4/8/16/32/64`，4點視窗用於保存
   快速暫態訊號。
2. raw與per-sample difference特徵分組校準，可選`raw`、`delta`或
   `raw_delta`，避免正常製程轉換期的raw誤差掩蓋delta證據。
3. normal-only reconstruction loss中，delta feature權重預設為2.0、
   elapsed phase權重為0.25、時間差分loss權重為0.5。
4. score mode固定搜尋`last/mean/max/delta_mean/delta_max`。
5. persistence固定搜尋`1-of-1/2-of-3/3-of-5`。
6. 所有告警採嚴格`score > threshold`，總事件FPR目標為0.1%。

以上變更不使用舊V3.2 locked holdout重新選參數；舊結果只用來定義新的研究
問題，因此V5必須使用全新selection、calibration及holdout seeds。

## 4. Selection gate

V5候選必須在selection cohort同時達到：

- FPR不高於0.1%。
- 整體Recall至少75%。
- F1至少85%。
- Type A Recall至少60%。
- Type B Recall至少70%。
- Type C Recall至少80%。

若selection gate未通過，不得開啟locked holdout。可以保留selection失敗
報告作為消融/負結果，但論文正式邊緣模型仍使用V4。

## 5. 最終採用規則

V5通過selection gate不代表一定取代V4。normal-only calibration後只執行一次
全新locked holdout，並完整報告FPR、Precision、Recall、F1、AUC、A/B/C
Recall、95% Wilson interval及偵測延遲。若locked結果未改善V3.2的主要弱點，
論文保留V4為正式原型，V5列為未成功優化實驗，不再利用該holdout調參。
